name: tdd_example
description: Flutter TDD Clean Architecture

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.19.4 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  get_it: ^7.2.0
  flutter_bloc: ^8.1.2
  equatable: ^2.0.5
  dartz: ^0.10.1
  connectivity: ^3.0.6
  http: ^0.13.5
  shared_preferences: ^2.0.20
  cupertino_icons: ^1.0.2
  mocktail: ^0.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

